# docs

Reference artifacts that sit next to the code implementing them:
- [`bootstrap.md`](bootstrap.md) — one-time, out-of-band setup (state bucket,
  Linode + Proxmox credentials, first `tofu init`)
- MS-A2 setup / optimization checklist
- k3s topology notes (1 server + 2 agents; quorum reasoning)
- public-exposure architecture decisions (Caddy/Traefik + Cloudflare)
