---
devbox_user: lightster

devbox_base_packages:
  - git
  - build-essential
  - curl
  - ca-certificates
  - unzip
  - mosh
  - bubblewrap
  - socat
  - zsh

# JetBrains indexing consumes inotify watches; the default (~8192) is too low.
devbox_inotify_max_user_watches: 524288
